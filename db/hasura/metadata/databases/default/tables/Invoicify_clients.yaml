table:
  schema: Invoicify
  name: clients
array_relationships:
  - name: invoices
    using:
      foreign_key_constraint_on:
        column: client_id
        table:
          schema: Invoicify
          name: invoices
